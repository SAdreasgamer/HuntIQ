"""
LLM provider implementations.

Each module in this package implements the LLMProvider
interface for a specific LLM service:

- openrouter: OpenRouter API (primary, supports free models)
- openai_compat: OpenAI-compatible API endpoints
- ollama: Local Ollama inference server
"""
