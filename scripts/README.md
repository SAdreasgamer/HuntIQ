# Scripts

This directory contains operational and utility scripts:

- `start.sh` — Start the application in development mode
- `start_worker.sh` — Start Celery workers
- `seed.sh` — Seed the database with initial data
- `migrate.sh` — Run database migrations
- `test.sh` — Run the test suite
- `lint.sh` — Run all linters and formatters
